namespace: bsctl

targetCustomizations:
- name: dev-internal
  helm:
    values:
      replicaCount: 8
  clusterSelector:
    matchLabels:
      environment: dev-internal

- name: prod-internal
  helm:
    values:
      replicaCount: 5
  clusterSelector:
    matchLabels:
      environment: prod-internal