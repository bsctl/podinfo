
defaultNamespace: bsctl
#dependsOn:
#  - name: applications-config-applications-rancher-logging-crd

helm:
  chart: ./chart
  releaseName: podinfo
  #valuesFiles:
  #  - chart/values.yaml
  #  - chart/values-prod.yaml

targetCustomizations:
- name: dev-internal
  helm:
    values:
      replicaCount: 0
  clusterSelector:
    matchLabels:
      environment: dev-internal
- name: testing
  helm:
    values:
      replicaCount: 0
  clusterSelector:
    matchLabels:
      environment: testing